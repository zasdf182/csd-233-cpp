{
    "version": "0.2.0",
    "configurations": [
        ////////////////////////////////////////////////////////////////////////
        {
            // Style
            "name": "Debug current C++ file",

            // Config
            "type": "lldb",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/build/currCppFile",
            "preLaunchTask": "BuildMacExe",
            "terminal": "integrated",

            // Args
            "args": ["build/wordlist.txt"]
        }
        ////////////////////////////////////////////////////////////////////////
    ]
}
